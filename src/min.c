#include <stdio.h>

int main() {
    int r[6] = {1, 2, 3, 4, 5, 6};
    for (int i = 0; i < 6; i++) {
        int tmp = array[i]
        if (tmp < 0) {
            printf("%d\n", tmp);
        }
    }
}